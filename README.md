# OBJ Mesh Analyzer

A small C++20 learning project for loading a Wavefront OBJ file and printing basic mesh information.

It loads `assets/cube.obj` and prints:

- shape count
- vertex count
- triangle count
- bounding box min, max, center, and size

Open the folder in CLion and run the `mesh_analyzer` target.
